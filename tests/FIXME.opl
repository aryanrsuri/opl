type Point = List * Int;
type Person = { name: Option * string, age: int};
type Team = { uuid: string, leader: Person, members: List * Person};

-- FIXME: does not parse 
let fc2 = {
    fc1
    with uuid = "2344-23598243-234329834",
};

-- FIXME: does not parse 
let front_text = fc.front;
let is_open = fc.open;


print(front_text);
print(is_open);
print(fc.uuid);

type Numbers = List * Int;
type ReLuResult = Option * int;

type Person = { 
    name: string,
    nick_name: Option * string,
};

let a = {
    name = "aryan",
    nick_name = None,
    age = Ok 24,
}